name: Docker Build

on: [push]

jobs:
  build:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v3
    

    - name: docker build
      run: |
        docker build -t artgallery .

    - name: docker push
      run: |
        docker push artgallery


          
          


