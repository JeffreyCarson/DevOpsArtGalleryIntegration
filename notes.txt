first, make sure XQuarts is closed

then, run socat

    socat TCP-LISTEN:6000,reuseaddr,fork UNIX-CLIENT:\"$DISPLAY\"

then

Build

    docker build -t artgallery .

Containerize

    docker run -e DISPLAY=<your ip>:0 artgallery

